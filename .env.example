# Copy to .env and adjust values per environment
# For Docker Compose, the app uses http://elasticsearch:9200 internally
ELASTICSEARCH_URL=http://elasticsearch:9200
# Change this in production
API_KEY=change-me-very-secret

# Optional: set PYTHONPATH when running locally outside Docker
# PYTHONPATH=src
