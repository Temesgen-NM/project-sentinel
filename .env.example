# Copy to .env and adjust values per environment
# For Docker Compose, the app uses http://elasticsearch:9200 internally
ELASTICSEARCH_URL=http://elasticsearch:9200

# API key for FastAPI (required by /api/v1/* intelligence endpoints)
# Change this in production
API_KEY=change-me-very-secret

# Optional: Elasticsearch basic auth (when Elastic security is enabled)
# ELASTICSEARCH_USERNAME=elastic
# ELASTICSEARCH_PASSWORD=your-password

# Optional: TLS verification (when using https:// ELASTICSEARCH_URL)
# ELASTICSEARCH_VERIFY_CERTS=true
# Path to CA bundle file (inside the container) if using a private CA
# ELASTICSEARCH_CA_CERTS=/app/certs/ca.crt

# Optional: set PYTHONPATH when running locally outside Docker
# PYTHONPATH=src
